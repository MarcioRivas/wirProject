import { Item, items } from '@/types'

export const getCommonModel = (): Promise<Item[]> => {
    // TODO: fetch mercadolibre items
    // TODO: fetch ebay items

    // TODO: create common model
    return Promise.resolve(items)
}
